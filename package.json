{
  "name": "on-the-fly-battle-maps",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "NODE_NO_WARNINGS=1 tap ./test/unit --no-cov --timeout 30000",
    "test:integration": "NODE_NO_WARNINGS=1 tap ./test/integration --no-cov --timeout 30000",
    "test:integration:update": "NODE_NO_WARNINGS=1 TAP_SNAPSHOT=1 tap ./test/integration --no-cov --timeout 30000",
    "start": "node dev.js",
    "dev": "nodemon dev.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1692.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "canvas": "^3.1.0",
    "fastify": "^5.2.1",
    "fastify-static": "^4",
    "nodemon": "^3.1.9",
    "tap": "^21.0.1",
    "tap-image-snapshot": "github:gingermusketeer/tap-image-snapshot"
  }
}
